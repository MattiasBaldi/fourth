import { fn } from "three/tsl";

// export circular =  fn(() => {
//   const dist = positionLocal.distance(uCursor);
//   const radius = float(15.0);
//   const strength = float(10.0);

//   // Use .mul(-1) instead of .neg() or .negate() to ensure compatibility
//   const deformation = exp(dist.pow(2).div(radius.pow(2)).mul(-10)).mul(strength);
//   const displacedPosition = positionLocal.add(vec3(0, 0, deformation));
// })
